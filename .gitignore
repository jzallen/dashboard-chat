# Dependencies
node_modules/

# Build outputs
dist/
.wrangler/

# Environment variables
.env
.env.local
.env.*.local
.dev.vars

# IDE
.idea/
.vscode/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# Logs
*.log
npm-debug.log*

# Turborepo
.turbo/

# TypeScript
*.tsbuildinfo

# Playwright
playwright-report/
test-results/

# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
venv/
.venv/
ENV/
env/
.pytest_cache/
.mypy_cache/
*.egg-info/
*.egg

# Backend
backend/.env

# Embedded databases
*.db
*.duckdb

# E2E seed data
e2e/.seed-data.json

# Core dumps (root-level only; does not affect frontend/src/core/)
/core

# Bazel (symlinks + directories)
bazel-bin
bazel-out
bazel-testlogs
bazel-dashboard_chat
bazel-dashboard-chat

# Gas Town session artifacts (written by gt session-start hook in polecat
# worktrees; should never be committed). Was causing recurring net-zero
# WIP+gt-p35 collateral commits on every polecat MR (refinery dc-wisp-nf18).
CLAUDE.local.md
.bazel-cache/
# Local bazel overrides (BuildBuddy API key etc) — see .bazelrc.user.example
.bazelrc.user

# Claude Code worktrees
.claude/worktrees/

# Local-only docs (superpowers plans, specs)
docs/superpowers/

# Temporary files
tmp/

# Gas City
.gc/
.beads/*
!.beads/config.yaml
hooks/
.runtime/
.dolt-data/
rigs.json
