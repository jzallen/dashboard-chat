echo "Running pre-push checks..."

# Run all tests via Bazel with fresh results but cached build environment
echo "Running tests via Bazel..."
bazel test //... --nocache_test_results || exit 1

echo "All pre-push checks passed."
