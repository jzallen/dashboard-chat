// SCAFFOLD: true — DISTILL RED scaffold for worker-tool-dispatch-refactor PR 3.
// Real implementation in PR 3 covers sortTable / filterTable /
// replaceColumnFilters / clearFilters. UI directives only — no backend calls.

export const __SCAFFOLD__ = true;

const NOT_IMPLEMENTED = "Not yet implemented — RED scaffold (DISTILL output for worker-tool-dispatch-refactor)";

export function makeSortTableDispatcher(): never { throw new Error(NOT_IMPLEMENTED); }
export function makeFilterTableDispatcher(): never { throw new Error(NOT_IMPLEMENTED); }
export function makeReplaceColumnFiltersDispatcher(): never { throw new Error(NOT_IMPLEMENTED); }
export function makeClearFiltersDispatcher(): never { throw new Error(NOT_IMPLEMENTED); }
