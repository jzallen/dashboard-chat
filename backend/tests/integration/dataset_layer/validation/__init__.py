"""Per-turn validation package — RED scaffold (created by DISTILL).

ADR-019 Option β: Pandera-based per-turn shape validation against the
harness's existing `TableState.df`. Sub-200ms acceptance budget;
sub-100ms typical (design.md §6 OQ4).
"""

__SCAFFOLD__ = True
