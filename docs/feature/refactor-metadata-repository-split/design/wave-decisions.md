<!-- DES-ENFORCEMENT : exempt -->
# Wave Decisions — `refactor-metadata-repository-split` (DESIGN)

Each row is a Design-Wave Decision (DWD) ratified in `design.md` §4–§7. Decisions are append-only; revisions add a new row.

| ID | Decision | Drivers | Alternatives rejected | Consequences | ADR |
|---|---|---|---|---|---|
| DWD-1 | Split `MetadataRepository` into 8 per-aggregate repository classes (Option α) | Test boundaries already match aggregate boundaries (8 per-aggregate test files exist); test friction relief; ISO 25010 maintainability/modularity/testability/analyzability | β — 3-group split (groupings unstable, mini-god-objects); γ — mixin decomposition (does not actually split the public surface) | 866 LOC → 8 files of ~80–150 LOC; mock surface per use-case test drops 4–7×; 8 new container properties | ADR-020 |
| DWD-2 | Introduce a transitional `_LegacyMetadataFacade` exposing the legacy 35-method surface via `repositories.metadata` | Zero call-site change in Phase A; revertable; ~25 use-case call sites kept working unchanged | Big-bang rewrite of all use cases in one PR (rejected — high risk, blocks parallel work) | One additional class lives ~one release cycle; emits `DeprecationWarning`; deleted in Phase C | ADR-020 |
| DWD-3 | Each new repository takes `RestrictedSession` via constructor (composition, not inheritance) | Preserves existing DI/IOC pattern; matches current `MetadataRepository.__init__`; SOLID-DIP | Mixin composition into one class (γ); module-level functions taking session (loses `RepositoryContainer` lazy-instantiation contract) | New repos are drop-in for the container's `partial(Cls, db)` registration | ADR-020 |
| DWD-4 | Decompose `MetadataRepositoryProtocol` into 8 per-aggregate Protocols (`ProjectRepositoryProtocol`, …); keep the wide Protocol as a deprecation alias for one cycle | Interface Segregation; narrower mock surface; matches per-aggregate test files | Single mega-Protocol unchanged (rejected — perpetuates the smell at type level); no Protocols at all (rejected — loses `mypy` enforcement) | 8 small Protocols; `MetadataRepositoryProtocol` remains as `Union` or alias documenting migration | ADR-020 |
| DWD-5 | Move per-aggregate helpers (`_encode_session_cursor`, `_decode_session_cursor` → `SessionRepository`; `_build_transform_record` → `TransformRepository`); lift `handle_repository_exceptions` decorator to `metadata/_base.py` | Helpers belong with their owning aggregate; decorator is cross-cutting | Keep helpers at module top-level (rejected — orphan helpers signal failed cohesion) | One additional shared module `_base.py`; helpers no longer importable across aggregates | ADR-020 |
| DWD-6 | Migration in three phases: A (additive, container + facade ship; tests green by construction) → B (8 incremental aggregate-by-aggregate use-case migration batches) → C (delete facade + legacy Protocol) | Each phase independently revertable; Phase B parallelizable; preserves Phase 2 dbt-test-validation read-only fence | Single-PR refactor (rejected — high revert cost); Phase A only (rejected — leaves facade indefinitely) | ADR-020 lists "all 8 aggregate batches landed" as Phase C entry criterion; tracked in roadmap at DELIVER kickoff | ADR-020 |
| DWD-7 | Add `pytest-archon` rule: `app.use_cases.*` MUST NOT import `MetadataRepository` or `_LegacyMetadataFacade` directly; pre-Phase-C the rule warns, post-Phase-C it errors | Architectural enforcement (principle 11); `pytest-archon` already in use per ADR-019; analog of "wire-then-probe-then-use" for the import boundary | Convention only (rejected — convention erodes); `import-linter` (rejected — same-rationale-as-ADR-019: `pytest-archon` already operationalized in the codebase) | One new test under `backend/tests/architecture/`; CI failure on legacy-import regression | ADR-020 |
| DWD-8 | Org-scoping discipline preserved unchanged at use-case/router boundary; each new repository accepts `org_id` as a parameter where the current methods do | CLAUDE.md constraint: every aggregate query is org-scoped; no security regression | Push org-scoping into the repos (rejected — couples repos to auth, breaks the "transactional consistency at router/controller level" rule in `RestrictedSession` docstring) | No security delta; routers/use cases keep enforcing tenancy | ADR-020 |
| DWD-9 | No new external integration; no DEVOPS contract-test annotation needed | Refactor is internal | — | DEVOPS handoff for this feature is empty for contract tests | — |
| DWD-10 | Effort estimate **M** (~4 days): Phase A 1d, Phase B 2d (8 batches × ~30min), Phase C 1d (incl. archunit-rule promotion + deprecation cleanup) | Hotspot review estimate; per-aggregate test layout already in place; mechanical conftest swaps | — | Roadmap-time slot: M | — |
