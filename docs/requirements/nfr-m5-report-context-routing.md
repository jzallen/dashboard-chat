# NFR-M5: Report Context Routing

## Tag

M5 — Model: Extensibility

## Invariant

> The agent SHALL support `contextType: "report"` alongside `"dataset"` and `"view"`, enabling the full modeling layer.

## Status

**Not implemented** — tracked in `report-chat-tools` proposal

## Verification Method

Verify that the chat agent correctly routes messages with `contextType: "report"` and invokes appropriate report-specific tools.

## Related

- `report-chat-tools` proposal
