{
  "name": "@dashboard-chat/tests-user-flow-state-machines",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "cucumber-js",
    "test:smoke": "cucumber-js --tags '@walking_skeleton'",
    "test:enabled": "cucumber-js --tags 'not @skip'",
    "compose:up": "docker compose -f ../../../docker-compose.yml --profile flow-state up -d",
    "compose:down": "docker compose -f ../../../docker-compose.yml --profile flow-state down -v"
  },
  "dependencies": {
    "@cucumber/cucumber": "^12.0.0",
    "@hono/node-server": "^1.14.0",
    "@vitest/expect": "^4.0.15",
    "expect": "^30.4.1",
    "hono": "^4.7.0",
    "ioredis": "^5.4.0",
    "jose": "^6.1.3",
    "undici": "^7.0.0"
  },
  "devDependencies": {
    "ts-node": "^10.9.2",
    "tsx": "^4.19.0",
    "typescript": "^5.6.3",
    "vitest": "^4.1.6"
  },
  "//layout": "TS acceptance suite per docs/feature/user-flow-state-machines/distill/wave-decisions.md §DWD-1. Standalone workspace, peer to the Python acceptance suites under tests/acceptance/. Run from inside this directory; not part of the root turbo graph (mirrors `cd tests/acceptance/<feature> && uv run --no-project pytest` posture)."
}
